# CoCodex implementation handoff — 2026-07-28

## Objective

Continue building the private-alpha CoCodex desktop product from the current working tree. Preserve all existing changes. The immediate remaining work is to rebuild and verify the Tauri installers with the new fail-safe proxy-injection guard, run full regressions, repair confirmed failures, and then intentionally commit/push the verified branch.

## Repository and tools

- Repository: `C:\Users\Stephen\Documents\Codex\2026-07-24\add-this-to-the-prompt-direct\outputs\cocodex`
- Branch: `feat/cocodex-foundation`
- Base HEAD before current uncommitted work: `c7f4f148`
- Portable Git: `C:\Users\Stephen\Documents\Codex\2026-07-24\add-this-to-the-prompt-direct\.portablegit\runtime\bin\git.exe`
- Repository Bun: `.\node_modules\.bin\bun.exe`
- Rust/Cargo: `C:\Users\Stephen\.cargo\bin\cargo.exe`

The worktree is intentionally dirty. Do not reset, discard, or overwrite unrelated modifications.

## Authoritative product direction

The mandatory private-alpha path remains:

1. Stephen starts the separate CoCodex Server.
2. Kai enrolls and connects.
3. Both open the same project and chronological shared chat.
4. Kai routes an agent task to Stephen's local client/account and receives streamed results.
5. Stephen routes an agent task to Kai's local client/account and receives streamed results.
6. Private messages are end-to-end encrypted and stored as ciphertext only.
7. Both clients recover after a server restart with ordered queued events.
8. Existing local OpenCodex/Codex behavior continues while the collaboration server is offline.

Do not spread work across secondary features until that path is fully connected and tested.

## Completed in the current working tree

### Tauri desktop runtime

- The existing web dashboard is packaged as a Tauri 2 desktop application.
- The Rust shell supervises a bundled `cocodex-runtime` sidecar on port `10100`.
- Tauri origins, lifecycle logging, sidecar build scripts, and compiled-process tests were added.
- Previous NSIS/MSI builds were manually exercised successfully, including process ownership and shutdown. Their hashes are stale because the source changed afterward; rebuild before distribution.
- Architecture/evidence: `docs/adr/0049-cocodex-tauri-managed-client-runtime.md` and `docs/evidence/tauri-managed-client-runtime.md`.

### Fail-safe proxy injection guard

- Normative contract: `docs/adr/0050-cocodex-fail-safe-proxy-injection.md`.
- `src/codex/injection-guard.ts` fails closed unless:
  - the listener is on the configured port;
  - `/healthz` identifies the proxy and reports a PID;
  - that PID passes expected-process verification; and
  - a viable, enabled, running persistent service or healthy enabled shim exists.
- `src/codex/inject.ts` saves an exact pre-attempt journal, performs atomic writes, re-verifies after writing, and restores on any failure.
- `ocx ensure` journals before spawning and stops only its own child when startup/readiness fails.
- Service installation journals before setup and rolls back newly created service state/processes when verification fails.
- Startup stale-injection recovery uses the exact journal while preserving post-injection user edits.

## Verification already completed

Commands were run from the repository root.

```powershell
.\node_modules\.bin\bun.exe test .\tests\codex-injection-guard.test.ts .\tests\codex-inject-integration.test.ts .\tests\codex-journal.test.ts .\tests\startup-prompt.test.ts .\tests\service.test.ts --timeout 30000
```

Result: exit `0`, 68 passed, 0 failed, 309 expectations.

```powershell
.\node_modules\.bin\bun.exe test .\tests\service.test.ts .\tests\autostart-health.test.ts .\tests\startup-action-control.test.ts .\tests\codex-runtime.test.ts .\tests\codex-shim.test.ts .\tests\codex-shim-autorestore.test.ts --timeout 30000
```

Result: exit `0`, 104 passed, 0 failed, 484 expectations.

```powershell
.\node_modules\.bin\bun.exe run typecheck
```

Result: exit `0`.

```powershell
git diff --check
```

Result: exit `0`.

An earlier broad suite reached 4,225 passes and 4 skips. One encrypted-project-context test timed out once and then passed three isolated retries. The final broad retry was interrupted, so a complete fresh run is still required.

## Important external process

At the last verified checkpoint, a user-installed OpenCodex process—not created by this work—was listening on port `10100`:

- PID at that checkpoint: `23976` (re-check; PID may have changed)
- Executable path: `C:\Users\Stephen\AppData\Roaming\npm\node_modules\@bitkyc08\opencodex\node_modules\bun\bin\bun.exe`
- Reported health version: `2.7.42`

Never stop or overwrite it merely because it occupies port `10100`. Re-inspect current ownership before testing. Use isolated ports/homes where possible.

## Required next steps

1. Inspect current git status, processes, port `10100`, installer artifacts, and any test workers before acting.
2. Review the fail-safe guard diff for transaction completeness, especially shim-install rollback and stale localhost recovery.
3. Add any missing targeted process-level tests without weakening existing tests.
4. Rebuild the Tauri sidecar and both Windows installers.
5. Recompute artifact hashes and update the evidence document; do not reuse the old hashes.
6. Test the packaged application with isolated `CODEX_HOME`, `OPENCODEX_HOME`, and ports wherever supported.
7. Run the complete existing and new test suites. Repair confirmed failures and rerun.
8. Run `git diff --check`, typecheck, relevant Rust tests, and installer smoke tests.
9. Produce evidence with test name, command, exit status, relevant output, process/network evidence, files, and commit SHA.
10. Only after verification, commit intentionally and push `feat/cocodex-foundation`. Do not rewrite history or discard user changes.

## Safety constraints

- Never leave Codex configured for `localhost:10100` without verified proxy health, expected process ownership, and a working startup mechanism.
- On setup failure, exact-restore native Codex, stop only processes created by the attempt, and remove only autostart changes created by that attempt.
- Do not delete user data, reset unrelated Codex settings, stop foreign processes, or overwrite unrelated repository changes.
- Do not claim a feature works from inspection alone. Use real process/network tests wherever practical.
- The CoCodex Client and CoCodex Server remain separate applications/processes.

## Git status at handoff

The current work includes Tauri runtime files, injection-guard implementation/tests, server/client changes, architecture decisions, and evidence. Run `git status --short` for the authoritative current list; no current changes have been committed beyond base HEAD `c7f4f148`.

## Latest checkpoint - persistent CLI sessions and fresh bundles

The requested Codex CLI efficiency behavior is now implemented:

- durable independent sessions scoped by project/chat/agent and all model,
  workspace, access, runtime, and trust-policy reset boundaries;
- related queued tasks resume the captured CLI session UUID;
- same-agent execution is serialized;
- controlled rotation occurs after 24 turns or a 180,000-token input turn;
- stale resumes are never automatically replayed;
- the stable prompt includes final goal, task/dependency IDs, and only
  explicitly selected accepted artifacts, not shared-chat history; and
- signed host/per-agent usage reports expose cached-input percentages in the UI.

A real two-turn adapter run measured 1,920 cached tokens on turn one and
23,296 on the resumed turn. See
`docs/evidence/codex-cli-session-efficiency.md`.

The first complete regression found three real failures: two PowerShell
installer diagnostics were not observable through Bun pipes, and the compiled
Codex fixture still required `--ephemeral`. The installer now emits
terminating diagnostics through `Console.Error`; the fixture models fresh
persistent and resumed sessions. The repaired isolated subset passed 7/7.
The subsequent complete suite passed 4,240 tests, 4 skipped, 0 failed, with
21,630 expectations across 353 files.

Additional green gates:

- explicit CoCodex suite: 177 passed, 0 failed;
- GUI: 130 passed, 0 failed; production build succeeded;
- root and CoCodex TypeScript checks: exit 0;
- Rust tests: 2 passed; Clippy with warnings denied: exit 0; and
- focused Tauri/ownership/security gate: 6 passed, 0 failed.

Fresh NSIS/MSI/sidecar hashes and isolated packaged lifecycle evidence are in
`docs/evidence/tauri-managed-client-runtime.md`. Foreign PID 23976 still owns
`127.0.0.1:10100` and was never stopped or adopted.

No commit or push has been made. Publication remains gated on the broader
product acceptance objective.

## Product continuation checkpoint - task dependency graph

After the green CLI-session and bundle checkpoint, the authoritative brief was
re-audited rather than treating the foundation as complete.

The desktop now renders an actual dependency graph from Server-authoritative
task state. It derives ready, waiting, failed, missing, and cyclic states,
layers dependent tasks, names upstream agents, and shows accepted artifact
handoffs with source task metadata. It does not mutate authority or dispatch.

Verification:

```text
focused graph: 3 passed, 0 failed, 14 expectations
complete GUI: 133 passed, 0 failed, 626 expectations
GUI lint: 0 errors, one pre-existing hook warning
GUI production build: exit 0
```

Evidence:

- `docs/evidence/task-dependency-graph.md`
- `docs/evidence/product-requirement-gap-matrix.md`

The requirement matrix records the remaining release blockers, including the
mature ratchet/private attachment/multi-device path, elevated helper, official
browser surface, complete co-agent policy/state, Git integration workflow,
Server service/update UX, signed distribution, and one uninterrupted 70-step
acceptance run.

Because this GUI slice follows the most recent NSIS/MSI build, those installer
hashes are now stale relative to source and must not be published as containing
the task graph. PID 23976 remains foreign and untouched. No commit or push has
been made.
## Authenticated private-message action substrate

The resident Client now supports a backward-compatible signed payload version
2 for message/reply/reaction/edit/delete events. Action metadata is inside the
sealed ciphertext and Ed25519 transcript; Server storage and routing remain
ciphertext-only. The Client rejects missing/cross-conversation targets,
self-targets, and edits/deletes by anyone other than the original sender before
persisting an incoming event. Existing version-1 encrypted history remains
readable. At this checkpoint it was an event substrate only; GUI projection and
controls were still pending and are covered by the later messaging-action
checkpoint. This does not claim X3DH/PQXDH, Double Ratchet, forward secrecy,
post-compromise recovery, attachments, or multi-device support.

Verification:

```text
focused private crypto/history/process: 11 passed, 0 failed, 317 expectations
TypeScript: exit 0
complete repository: 4,240 passed, 4 skipped, 1 failed, 21,633 expectations
isolated rerun of sole project-context failure: 3 passed, 0 failed, 81 expectations
```

The sole full-run failure was the unrelated encrypted project-context session
timing test; its exact three-test file passed immediately in isolation. Treat
the complete release gate as not yet fully green and rerun it before release.
The foreign listener remains PID 23976 on 127.0.0.1:10100 and was not stopped
or adopted. Installers remain stale after source changes. No commit, push, or
publish has been made.
## Final green release-gate checkpoint

A second complete audit found and repaired two gate defects: the privacy scan
mistook Tauri's required high-resolution icon filename for an email, and an unowned
`AbortSignal.timeout()` in proxy liveness could intermittently keep a Bun CLI
child alive after models persistence completed. The scanner now narrowly
allowlists that exact standard asset with a regression test. Liveness now owns
and unconditionally clears/aborts its probe; the CLI process fixture also has a
hard child timeout.

Final green evidence:

```text
complete repository: 4,243 passed, 4 skipped, 0 failed, 21,639 expectations
explicit CoCodex: 178 passed, 0 failed, 2,051 expectations
GUI: 133 passed, 0 failed, 626 expectations
privacy scan: passed
focused privacy/service/package/fail-safe: 123 passed, 0 failed, 1,122 expectations
Rust: 2 passed; Clippy -D warnings: exit 0
TypeScript, GUI production build, and GUI lint: exit 0
```

The final rebuilt NSIS/MSI/runtime hashes and repeated isolated packaged
lifecycle evidence are in `docs/evidence/tauri-managed-client-runtime.md`.
Both packaged variants rejected foreign PID 23976, spawned only WebView2, and
started no bundled runtime. PID 23976 remains the sole 10100 listener and was
never stopped or adopted.

The release-gate checkpoint is green, but the full product brief is not
complete. Remaining requirements in the gap matrix still block any claim of
finished product or public release. The private-message action substrate now has
verified GUI projection and controls. The later typing checkpoint completes
ephemeral private typing indicators; mature ratcheting, multi-device
attachments remain separate unimplemented requirements. Native desktop notifications are implemented and package-verified below; one user-observed OS toast remains manual acceptance.

## Private-message GUI actions

The desktop now projects immutable private message/reply/reaction/edit/delete
events and exposes reply, thumbs-up reaction toggle, owner-only edit, and
owner-only delete controls. Deleted plaintext is removed from the projection,
cross-conversation actions and unauthorized edit/delete events are ignored, and
the UI routes every action through exact tested resident-client command
constructors.

Verification:

```text
focused projection/command tests: 4 passed, 0 failed, 10 expectations
complete GUI: 137 passed, 0 failed, 636 expectations
GUI lint: 0 errors, one pre-existing hook warning
GUI TypeScript and production Vite build: exit 0; 146 modules transformed
```

Evidence: `docs/evidence/private-message-actions.md`.

These GUI changes follow the last Tauri bundle build, so the recorded
NSIS/MSI/runtime hashes are stale relative to source and must not be described
as containing this messaging UI. Foreign PID 23976 remains the sole
`127.0.0.1:10100` listener and was not stopped or adopted.
## Private typing indicators

Private typing now has a dedicated authenticated protocol path from the resident
Client through CoCodex Server to the recipient Client and desktop. It is
recipient-only, approved-device validated, self-target rejecting, rate-limited,
never queued, and never persisted. The desktop sends one start signal, clears it
after 1.8 seconds idle and on send/contact/disconnect, and expires a missing
remote stop after 5 seconds. The visible three-dot status is localized,
accessible, and reduced-motion safe.

Verification:

```text
protocol: 24 passed, 0 failed, 164 expectations
real WSS Server: 8 passed, 0 failed, 129 expectations
compiled three-process private alpha: 1 passed, 0 failed, 290 expectations
GUI bridge/redaction: 1 passed, 0 failed, 113 expectations
private state: 4 passed, 0 failed, 12 expectations
rendered chat shell: 5 passed, 0 failed, 20 expectations
explicit CoCodex main group: 178 passed, 0 failed, 2,061 expectations
complete GUI: 138 passed, 0 failed, 643 expectations
CoCodex TypeScript, GUI build, lint, localization, privacy scan: passed
complete repository rerun: all 354 files passed across 15 fresh workers
```

The first complete batched attempt reported one failed worker but its
failure-focused tail did not retain an identifiable test. No test process
remained. An identical second complete run passed every worker and file. No
source change was made to conceal or skip that result.

Evidence: `docs/evidence/private-message-typing.md`.

The existing Tauri installers predate both the private action GUI and typing
changes; their hashes must not be published as containing either slice. PID
23976 remains the sole `127.0.0.1:10100` listener and was never stopped or
adopted.
## Provider-readiness and home-protection checkpoint

The live Windows installation was protected without running `ocx init`,
changing account mode, stopping the service, or touching its listener. Exact
backup and verification evidence is in
`docs/evidence/fail-safe-readiness-and-home-protection.md`. At this checkpoint
the installed scheduled task is Running, `/healthz` is OK, account mode is
`direct`, and user-installed OpenCodex PID 3704 owns
`127.0.0.1:10100`. Treat that listener as foreign to repository tests.

The repository now adds a fail-closed `/readyz` proof for proxy,
configuration, canonical OpenAI provider, and usable Direct/Pool credentials.
Injection requires matching `/healthz` and `/readyz` PIDs plus operational
service/shim autostart before writing `openai_base_url`. Missing, expired, and
Pool-empty authentication fail before injection; Pool-empty setup offers
Direct mode when usable or cancellation. Existing journal restoration remains
the rollback boundary.

Initialization preserves explicit `codexAccountMode`. CLI, GUI-worker, and
Windows npm-launcher updates create collision-safe exact config backups before
stop/package replacement. Focused verification is green:

```text
readiness/injection/journal/init/backup: 52 passed, 0 failed
real-process/service/reboot/update lifecycle: 111 passed, 0 failed
npm launcher syntax: exit 0
TypeScript and git diff --check: exit 0
```

A real isolated Direct-mode runtime returned HTTP 200 from `/readyz` with all
four checks true. Real isolated missing/expired credential children returned
HTTP 503. PID 3704 remained the sole port-10100 listener.

These source changes postdate the latest MSI/NSIS build. All previous installer
hashes are stale for this checkpoint. Re-run the full repository/GUI/Rust/privacy
gates and rebuild/re-smoke both installers before any commit, push, or publish.
## Native desktop notification and final fail-safe bundle checkpoint

Private-message notifications are now explicit opt-in native Tauri
notifications. Dispatch excludes message plaintext, restored/action/sent
events, and the focused active conversation. Permission denial, storage
failure, and native delivery exceptions fail closed. The main capability
contains only `core:default` and `notification:default`; shell execution
remains forbidden. Evidence:
`docs/evidence/private-message-notifications.md`.

After the notification and provider-readiness source changes, the final
complete suite passed 4,260 tests, 4 skipped, 0 failed, with 21,666 expectations
across 357 files and 15 fresh workers. Explicit CoCodex passed 178/0/2,061;
GUI passed 142/0/661. TypeScript, privacy, GUI build/lint/localization, Rust,
and Clippy all passed. One earlier complete run found the stale Tauri
capability snapshot and one unrelated Windows temporary-directory `EBUSY`
teardown. The snapshot was corrected; the revocation product test passed three
fresh isolated runs and the later complete suite was fully green.

Fresh installers were rebuilt and re-smoked. Authoritative hashes are in
`docs/evidence/tauri-managed-client-runtime.md`. NSIS and MSI packaged
desktops both rejected foreign user-installed PID 3704 on port 10100, spawned
only WebView2, started no bundled runtime, and left PID 3704 listening after
exact desktop cleanup. NSIS silent uninstall exited 0 and removed its isolated
install root. MSI administrative extraction exited 0.

Native OS notification permission/toast interaction remains a manual
user-observed acceptance because permission was not clicked automatically.
Do not claim that visible toast step as tested.

## Windows desktop recovery shortcut checkpoint

A one-click Windows recovery control now lives at
`C:\Users\Stephen\Desktop\Repair Codex.lnk`. Its repository source is
`bin/repair-codex.ps1`, and the installed copy is
`C:\Users\Stephen\.opencodex\repair-codex.ps1`. The shortcut backs up config
without overwriting an existing backup, inspects rather than stops unknown
listeners, starts the service only on an empty configured port, requires
credential-bearing `/readyz` before `ocx ensure`, and restores native Codex on
any unproven readiness or repair failure. It never runs `ocx init`.

The source/installed script hashes match at
`C1EF412F66F3B9FB1B89B373304781A69B9A44CB9B13788BD877CA1F1C8C151B`. The
focused contract suite passed, and an isolated configured-port-19099 smoke
proved exact backup hashing, service-start-then-native-restore behavior, and
no `ensure` after readiness failure. A live dry run detected foreign PID 3704
on port 10100 and left it running; the home service stayed Running, health was
OK, and `codexAccountMode` stayed `direct`. Full evidence is in
`docs/evidence/windows-repair-shortcut.md`.

## Git integration workflow continuation checkpoint

The local Client now has an explicit fail-closed Git integration coordinator in
`src/cocodex/git-integration.ts`, with `cocodex git-preview` and
`cocodex git-integrate` CLI commands. Preview verifies registered worktree
ownership, clean task/target state, branch/base ancestry, expected target
commit, peer changed-path overlap, and read-only `git merge-tree` conflicts.
Blocked work returns a revision-required integration artifact; only a clean,
explicit recheck can perform a no-ff merge and return an integrated artifact.
No worktree or branch is deleted automatically.

Verification: `tests/cocodex-git-integration.test.ts` passed 3/0 with 18
expectations, repository TypeScript passed, and CLI help exposed both commands.
Temporary real Git repositories were removed. Server artifact publication,
GUI integration controls, and inclusion in the one uninterrupted 70-step run
remain outstanding; the product is not complete and no release claim is made.

## Fresh Git-integration release-gate checkpoint

After the Git integration module and CLI were added, the complete repository
regression passed `4,265` tests with `4` skips and `0` failures (`21,696`
expectations across `359` files). CoCodex passed `178/0/2,061`; CoCodex
TypeScript, GUI build/lint, privacy, Rust, and Clippy all passed.

A fresh `bun run build:tauri` succeeded. Current artifact hashes and isolated
NSIS/MSI ownership/recovery evidence are in
`docs/evidence/tauri-managed-client-runtime.md`. Both packaged variants
rejected foreign PID `3704`, spawned only WebView2, launched no bundled
runtime, and left `127.0.0.1:10100` owned by PID `3704` after exact cleanup.
NSIS uninstall and MSI administrative extraction both exited `0`; the MSI
runtime hash matched the fresh bundled runtime.

The product remains incomplete against the authoritative brief. The local Git
preview/integration slice is verified, but Server publication/GUI controls,
mature ratcheted messaging, attachments/multi-device, elevated helper,
browser surface, server service/update, signing, and the single 70-step run
remain outstanding. Do not publish or mark the goal complete.
## Resident Client/GUI Git integration and current-build release checkpoint

The Git workflow is now wired through the resident Client and chat-first GUI,
not only the CLI. The resident command derives task/worktree paths from the
server task projection plus the local agent policy/ownership registry; it
rejects renderer-supplied paths and requires a fresh target-commit preview
before integration. Successful and revision-required outcomes publish through
the existing encrypted project artifact/outbox route. The GUI exposes explicit
review and integrate controls for completed local Git-worktree tasks.

Current gates:

complete repository: 4,265 passed, 4 skipped, 0 failed; 21,702 expectations; 359 files
dedicated CoCodex: 178 passed, 0 failed; 2,067 expectations; 37 files
focused Git integration: 3 passed, 0 failed; 18 expectations
focused GUI bridge: 1 passed, 0 failed; 119 expectations
CoCodex TypeScript: exit 0
GUI lint: 0 errors, one pre-existing hook warning
GUI/package build: exit 0
privacy scan: passed
Tauri Rust tests: 2 passed, 0 failed
Clippy -D warnings: exit 0

bun run build:tauri then produced fresh NSIS/MSI/sidecar/desktop artifacts.
The authoritative current hashes, exact installer payload hashes, and current
NSIS/MSI ownership smoke are recorded in
docs/evidence/tauri-managed-client-runtime.md. Both package variants rejected
foreign PID 3704, spawned only WebView2, started no bundled runtime, and left
port 10100 owned by PID 3704 after exact cleanup.

This supersedes the earlier Git checkpoint sentence that Server publication
and GUI controls were outstanding: publication now uses the existing encrypted
artifact route and the controls are implemented. The product is still not
complete against the authoritative brief. The one uninterrupted 70-step
scenario, mature ratcheted messaging, private attachments/multi-device,
elevated helper, browser capability, optional Server service/update UX,
signing/automatic updates, and full reference-level acceptance remain
outstanding. Do not publish or mark the goal complete.
## Final source/package freeze checkpoint

Final full regression: 4,265 passed, 4 skipped, 0 failed; 21,702 expectations
across 359 files. Final dedicated CoCodex: 178 passed, 0 failed; 2,067
expectations across 37 files. Focused Git/GUI: 4 passed, 0 failed; 137
expectations. TypeScript, GUI lint/build, privacy, Rust tests, and Clippy
passed; GUI lint has one pre-existing hook warning.

Final current-build hashes and both current NSIS/MSI smoke records are in
docs/evidence/tauri-managed-client-runtime.md. Both variants rejected foreign
PID 3704, launched only WebView2, started no bundled runtime, and left port
10100 with PID 3704 after exact cleanup. The home scheduled task remained
Running, health remained OK, mode remained direct, and the Repair Codex
shortcut remained installed.

This is a tested, intentionally scoped continuation checkpoint—not a claim
that the entire authoritative product brief is complete. The one uninterrupted
70-step scenario, mature ratcheted messaging, private attachments/multi-device,
elevated helper, browser capability, optional Server service/update UX,
signing/automatic updates, and full reference-level acceptance remain
outstanding. Do not publish or mark the goal complete.

## Optional Server service and installed-package checkpoint

Commit `a5e84476ad5e94cf00d1b838c45471c09ac62bea` implements the
optional Windows service required by the brief through the standalone
`cocodex-server service install|start|stop|status|uninstall` surface. It uses
the distinct SCM id `cocodex-server`, pinned WinSW 2.12.0, same-domain/user
DPAPI custody, exact registered binary/state-root checks, automatic delayed
startup, TLS readiness, transactional fresh-install cleanup, prior-state repair
rollback, stale-asset SCM cleanup, and no OpenCodex/10100 coupling. ADR 0052,
the Server guide, focused regressions, and full evidence are committed.

Final service-focused tests passed 17/0/69; the complete standalone Server
suite passed 80/0/904. The final post-verifier complete repository pass succeeded with 4,265
passes, 4 skips, 0 failures, and 21,704 expectations across 359 files. GUI
passed 142/0/661 plus production build; Rust passed 2/0; Clippy, CoCodex/root
typechecks, privacy, and diff checks passed. Two real test defects exposed by
the release gates were repaired: bounded Tauri sidecar fetches under full-suite
concurrency and CRLF-independent GUI scope-state inspection.

The clean-commit private-alpha archive SHA-256 is
`09dd139701900f2616f4eb310653e6a5fcc0c22dd4f3ee3d4497903313aa3c9c`.
The authoritative installer accepted all 126 locked dependencies and the
installed verifier passed isolated local runtime/GUI and Server
init/restart/stop. Verifier cleanup now targets only its exact subprocess, so
the user's installed OpenCodex service metadata cannot redirect test cleanup.
Home PID 3704 remained Running/healthy on 127.0.0.1:10100 and OpenAI mode
remained `direct`.

No live `cocodex-server` SCM registration was created because that operation
requires the operator's Windows credentials/UAC. Live packaged SCM/reboot
acceptance and the separate verified Server update UX remain outstanding.
Continue with the update UX or another gap-matrix blocker; do not redo the
service implementation, do not claim live SCM acceptance, and do not mark the
full product goal complete.

## Verified Server update and fresh desktop package checkpoint

Commit d77dec10c9b6b54f836b34efd470a113cf88ae6b implements ADR 0053:
installed-only Server update-check, bounded independent verification of all
four release inputs, installer Check, direct/SCM/shared-process readiness,
preserved-state disclosure, and exact shell-free external Update argv.

Final complete regression after the process-guard correction: 4,265 passed,
4 skipped, 0 failed, 21,713 expectations across 359 files in 319 seconds.
Focused package/update tests passed 14/0/526. The standalone Server suite
passed 88/0/936, maintained CoCodex passed 203/0/2,181, and TypeScript,
GUI 142/0/661 plus build/lint, Rust 2/0, Clippy, and privacy gates passed.

Clean-commit private-alpha archive:
0150496bf85873b152a0bbb201f90226592d6968a1f2f00c7104076401d3e31f.
The installed update-check reported ready in an isolated prefix; its exact
external argv completed Update; all four state canaries survived update and
uninstall; and the immediate post-update package verifier passed locked
dependencies, local runtime/GUI, and Server init/start/restart/stop. One
initial post-install verifier restart failed, then four subsequent fresh-state
lifecycle passes succeeded; the transient is recorded in dedicated evidence.

Fresh Tauri hashes:

    NSIS    615afc8eb4454ffb17a80f471f8abb391d50737ba1a90695d4fe0782daca047d
    MSI     3ed7427463435a52b8aaea3ea27f3288e792a98988ce4149942ad56a4ddb22a7
    runtime 9020b0eb464160d9385aaa10a8e7c6711777af9c90d09d1f654292033d0d9a23

Both fresh installer variants passed isolated ownership smokes, launched only
WebView2, started no bundled runtime while foreign PID 3704 owned port 10100,
logged the foreign-owner rejection, and were cleaned by exact verified paths.
The home listener remained PID 3704 and OpenAI mode remained direct.

No live SCM/UAC/reboot acceptance was performed. Signing/automatic update,
mature ratcheted multi-device attachments, elevated helper, browser capability,
reference-level visual QA, and the single uninterrupted 70-step run remain
release blockers. Do not mark the full product goal complete.
